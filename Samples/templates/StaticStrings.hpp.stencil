// Autogenerated file - Do Not Edit!
//
// To change values, edit the associated yaml file
//
// {{ name }}.hpp
//

#ifndef {{ name }}_hpp
#define {{ name }}_hpp

#include <stdio.h>
#include <string>

struct {{ name }} {
{% for group in groups %}
  struct {{ group.name }} { {% for string in group.strings %}
    static std::string {{ string.var }}; {% endfor %}
  };
{% endfor %}
};

#endif
