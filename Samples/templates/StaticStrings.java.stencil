package {{ package }};
import org.intellij.lang.annotations.MagicConstant;

/*
    Autogenerated file - Do Not Edit!
    To change values, edit the associated yaml file

    {{ name }}.java
*/

public final class  {{ name }} {
{% for group in groups %}
    @MagicConstant(valuesFromClass = {{ group.name }}.class)
    public @interface {{ group.name }} { {% for string in group.strings %}
        String {{ string.var }} = "{{ string.string }}"; {% endfor %}
    }
{% endfor %}
}
